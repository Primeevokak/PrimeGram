<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-path name="media" path="."/>
    <files-path name="logs" path="/logs/"/>
    <files-path name="cache" path="/cache/"/>
    <!-- PrimeGram: PrimePluginsController.pluginsDir() keeps .plugin files here; sharing one goes
         through this same FileProvider, and an uncovered path makes getUriForFile throw. -->
    <files-path name="plugins" path="/plugins/"/>
    <root-path name="external_files" path="/storage/" />
</paths>