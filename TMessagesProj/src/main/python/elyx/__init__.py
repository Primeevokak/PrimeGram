"""PrimeGram: the runtime surface a packaged (.elyx) plugin's own code imports from, mirroring
the ``elyx`` module elyxbuilder's real toolchain provides - see ``elyx.strings`` for the one piece
of it plugins actually call (confirmed by disassembling a real .elyx's compiled bytecode).
"""
